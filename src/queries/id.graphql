query Id($id: Int!) {
  nhentai {
    by(id: $id) {
      id
      title {
        japanese
        english
        pretty
      }
      numPages
      uploadDate
      tags {
        id
        name
        type
      }
    }
  }
}
